# Employer Assessment Carousel: Deferred QA

Status: export composition corrected; small Figma content changes deferred.

## Preserve

- The red horizontal rule on the review screen is intentional.
- Keep the sequence: configure assessment, review and create, manage assessments.
- Preserve historical product branding shown in the source frames.

## Revisit In Figma

### Slide 1: assessment setup (`15510:3795`)

- Replace both instances of `Placeholder for more info` with final helper text or remove them.
- Revise `Personalize your roles and offers sent to talents` so it describes assessment creation rather than roles and offers.

### Slide 2: review and create (`15510:3915`)

- Change `Take a last look at your information before sending` to creation-specific language.
- Review the talent-facing description under `Practical assessment`; it currently describes becoming discoverable to employers inside an employer workflow.
- Keep the `Create assessment` CTA and intentional red horizontal rule.

### Slide 3: assessment management (`15514:4854`)

- Review final dashboard labels and sample data for portfolio-ready copy.

## Re-export Acceptance

- Export lossless PNGs from the complete source frames.
- Resize proportionally to `1360px` wide.
- For a static slide, crop from the top to exactly `1360 x 850`; do not fit by height or add side margins.
- For a slide whose primary action or management content extends below the crop, retain the full `1360px`-wide export and enable the fixed-viewport scroll reveal.
- Verify again at the website display size of `680 x 425`.

## Code Follow-up

- Add the historical product stage to the visible captions after the Figma copy is final.
- Announce the active slide description or caption to screen readers, not only the slide counter.
